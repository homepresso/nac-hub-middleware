{
  "name": "expense_report_status",
  "description": "Retrieves the status of the most recent expense report the user has submitted.",
  "parameters": {
    "type": "object",
    "properties": {
      "email": {
        "type": "string",
        "description": "The email address of the user making the request."
      },
      "reqId": {
        "type": "string",
        "description": "The request ID of the expense report being checked."
      }
    },
    "required": ["email", "reqId"]
  }
}
